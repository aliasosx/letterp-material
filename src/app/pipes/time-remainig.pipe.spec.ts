import { TimeRemainigPipe } from './time-remainig.pipe';

describe('TimeRemainigPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeRemainigPipe();
    expect(pipe).toBeTruthy();
  });
});
