import { CustomerTelePhoneFilterPipe } from './customer-tele-phone-filter.pipe';

describe('CustomerTelePhoneFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomerTelePhoneFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
